<!DOCTYPE html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tech Blog</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.3/css/bulma.min.css">
    {{!-- <link rel="stylesheet" href="/node_modules/bulma/css/bulma.css" /> --}}
    <link rel="stylesheet" href="/stylesheets/style.css" />

  </head>
  <body>
    <div class="wrapper">
      <header class="hero is-small is-dark">
        <div class="hero-head">
          <nav class="navbar" role="navigation" aria-label="main navigation">
            <div class="navbar-menu">
                <a href="/" class="navbar-item">Home</a>
                {{#if loggedIn}}
                <a href="/dashboard" class="navbar-item">Dashboard</a>
                {{/if}}
                <div class="navbar-item">
                  <div class="buttons">
                    {{#if loggedIn}}
                      <button id="logout" class="button is-light">logout</button>
                    {{else}}
                      <a href="/login" class="button is-light">Login</a>
                      <a href="/signup" class="button is-primary">Sign up</a>
                    {{/if}}
                  </div>
                </div>
            </div>
          </nav>
          <div class="hero-body">
            <div class="container has-background-grey-lighter box is-rounded">
              <a href="/" class="title is-dark">The Tech Blog</a>
            </div>
          </div>
        </div>
      </header>
      <main class="has-background-grey-lighter"> {{{body}}}</main>
      <footer>Thank You for visiting Technology blog!</footer>
    </div>
    {{#if loggedIn}}
      <script src="/javascript/logout.js"></script>
    {{/if}}
  </body>
</html>